'use strict';

var APP = window.APP = window.APP || {};

APP.menu = (function () {
    
    var init = function (element) {
        console.log('APP.menu');
    };

    return {
        init: init
    };

}());